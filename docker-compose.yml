# Local production-like test: docker compose up --build
services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    environment:
      ENVIRONMENT: production
      PORT: 8000
    volumes:
      - uploads_data:/app/backend/uploads

volumes:
  uploads_data:
